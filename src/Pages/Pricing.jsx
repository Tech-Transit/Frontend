import React from "react";
import { Box, Typography } from "@mui/material";

const Pricing = () => {
  return (
    <Box sx={{ padding: 3 }}>
      <Typography variant="h4">Pricing</Typography>
      <Typography>Choose the best plan for your business.</Typography>
    </Box>
  );
};

export default Pricing;
